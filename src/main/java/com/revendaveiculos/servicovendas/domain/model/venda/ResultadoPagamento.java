package com.revendaveiculos.servicovendas.domain.model.venda;

/** Resultado informado pela entidade de pagamento externa via webhook. */
public enum ResultadoPagamento {
    APROVADO,
    CANCELADO
}
